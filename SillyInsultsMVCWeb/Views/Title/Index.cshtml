@model IEnumerable<Models.TitleDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TitleWord)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TitleWord)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TitleID })
            @Html.ActionLink("Delete", "Delete", new { id=item.TitleID })
        </td>
    </tr>
}

</table>
