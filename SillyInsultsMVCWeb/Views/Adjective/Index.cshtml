@model IEnumerable<Models.AdjectiveDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AdjectiveWord)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AdjectiveWord)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AdjectiveID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AdjectiveID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AdjectiveID })
        </td>
    </tr>
}

</table>
